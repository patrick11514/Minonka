import { Region } from '$/lib/Riot/types';

export type DefaultParameters = {
    summonerId: string;
    region: Region;
    level: number;
    gameName: string;
    tagLine: string;
    profileIconId: number;
};
